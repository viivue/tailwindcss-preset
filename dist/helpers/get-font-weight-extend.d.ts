import { ConfigTypeTheme } from "../types/theme-config";
/**
 * Add specific set of font weight for each font family
 * @returns {{}}
 */
export declare const getFontWeightExtend: (themeConfig: ConfigTypeTheme) => Record<string, string | number>;
